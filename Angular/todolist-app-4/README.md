1. adding a component Todos
2. using *ngFor to fetch list  from component to html 
3. push data from parent to child component
4. use of [ngclass]  to add css classes dynamically
5. add  (change) and (click) events